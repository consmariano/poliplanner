services:
  - name: web
    envVars:
      - key: DJANGO_SETTINGS_MODULE
        value: "blog.settings"
    buildCommand: "python manage.py migrate && python manage.py collectstatic --noinput"
    startCommand: "gunicorn blog.wsgi:application -b 127.0.0.1:$PORT"